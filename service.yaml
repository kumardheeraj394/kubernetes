apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: nginix-web
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 8081
  type: NodePort
